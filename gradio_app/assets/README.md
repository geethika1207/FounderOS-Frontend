No logo.png is bundled — the sidebar/header brand mark is a CSS badge
(see `.fos-brand-mark` in styles.css), so there's nothing to swap in.
Drop a `logo.png` here and reference it in `components.py` if you'd
rather use an image mark.
